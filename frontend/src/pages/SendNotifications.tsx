import React from 'react';

const SendNotifications: React.FC = () => {
  return (
    <div>
      <h1>Send Notifications</h1>
      <p>This is where you can send updates or reminders to volunteers.</p>
      {/* Add features for sending notifications */}
    </div>
  );
};

export default SendNotifications;
